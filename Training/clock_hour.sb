If (Clock.Hour < 12) Then
  TextWindow.WriteLine("Good Morning World")
Else 
    If (Clock.Hour > 12) Then
      TextWindow.WriteLine("Good Evening World")
    EndIf
  TextWindow.WriteLine("Noon World")
EndIf